{
  "$schema": "https://json.schemastore.org/tsconfig",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "allowJs": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "isolatedModules": true,
    "incremental": true,
    "jsx": "preserve",
    "paths": {
      "@needed/shared": ["./packages/shared/src"],
      "@needed/ui": ["./packages/ui/src"],
      "@needed/config/*": ["./packages/config/*"]
    }
  },
  // This file is intended to be extended by other tsconfigs.
  // Prevent editor/tsserver from treating it as an active project (which would
  // otherwise include the whole repo and can cause confusing resolution errors).
  "include": [],
  "exclude": ["node_modules", "dist", ".next", "build"]
}
